package com.example.pmdm_pokedex_composable.ui.theme

import androidx.compose.ui.graphics.Color


val White = Color(0xFFFFFFFF)           // Blanco puro para el fondo claro

val Teal200 = Color(0xFF80CBC4)         // Color principal (verde-azul suave)
val Teal700 = Color(0xFF00796B)         // Variante más oscura del verde-azul
val Lime500 = Color(0xFFCDDC39)         // Acento en verde lima (muy vibrante)
val Lime600 = Color(0xFFAFB42B)         // Acento verde lima más saturado

val Amber500 = Color(0xFFFFC107)        // Acento cálido (amarillo dorado)
val Amber600 = Color(0xFFFFB300)        // Amarillo anaranjado

// Tonos oscuros para contraste:
val Charcoal = Color(0xFF333333)        // Fondo oscuro (gris oscuro con un toque más cálido)
val DarkSlate = Color(0xFF2F3B4C)       // Superficies elevadas (azul grisáceo oscuro)
val DarkRed = Color(0xFFB71C1C)         // Rojo oscuro (para resaltar elementos importantes)

// Neutros:
val OffWhite = Color(0xFFF5F5F5)        // Blanco suave para fondos más cómodos
val LightSlate = Color(0xFF788C99)      // Gris azulado suave, ideal para superficies elevadas
val Black = Color(0xFF000000)           // Texto negro para alto contraste


val Purple200 = Color(0xFFCE93D8)       // Morado suave y claro
val Purple700 = Color(0xFF6A1B9A)       // Morado oscuro vibrante
val Lavender500 = Color(0xFFE1BEE7)     // Acento lavanda suave
val Lavender600 = Color(0xFFD1C4E9)     // Lavanda ligeramente más saturada

val Magenta500 = Color(0xFFEC407A)      // Acento magenta cálido
val Magenta700 = Color(0xFFD81B60)      // Magenta más oscuro para contraste

// Tonos oscuros:
val DeepPurple = Color(0xFF311B92)      // Fondo oscuro profundo
val DarkPlum = Color(0xFF4A148C)        // Superficies elevadas (ciruela oscura)

// Neutros:
val Ivory = Color(0xFFFFF8E1)           // Blanco cálido para el fondo claro
val SoftGray = Color(0xFFE0E0E0)        // Gris claro para superficies
val Onyx = Color(0xFF212121)            // Negro profundo para alto contraste

val Yellow200 = Color(0xFFFFF59D)       // Amarillo pastel suave
val Yellow700 = Color(0xFFFBC02D)       // Amarillo oscuro dorado
val Gold500 = Color(0xFFFFD700)         // Acento en dorado brillante
val Gold600 = Color(0xFFFFC107)         // Dorado cálido más saturado

val Orange500 = Color(0xFFFF7043)       // Acento en naranja cálido
val Orange700 = Color(0xFFEF6C00)       // Naranja más oscuro para contraste

// Tonos oscuros:
val DeepBrown = Color(0xFF3E2723)       // Fondo oscuro marrón profundo
val RichAmber = Color(0xFF795548)       // Superficies elevadas (ámbar oscuro)

// Neutros:
val Vanilla = Color(0xFFFFF8E1)         // Blanco cálido para el fondo claro
val LightTan = Color(0xFFD7CCC8)        // Beige claro para superficies
val Midnight = Color(0xFF212121)        // Negro profundo para alto contraste