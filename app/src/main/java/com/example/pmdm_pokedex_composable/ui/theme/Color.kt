package com.example.pmdm_pokedex_composable.ui.theme

import androidx.compose.ui.graphics.Color


val White = Color(0xFFFFFFFF)           // Blanco puro para el fondo claro

val Teal200 = Color(0xFF80CBC4)         // Color principal (verde-azul suave)
val Teal700 = Color(0xFF00796B)         // Variante más oscura del verde-azul
val Lime500 = Color(0xFFCDDC39)         // Acento en verde lima (muy vibrante)
val Lime600 = Color(0xFFAFB42B)         // Acento verde lima más saturado

val Amber500 = Color(0xFFFFC107)        // Acento cálido (amarillo dorado)
val Amber600 = Color(0xFFFFB300)        // Amarillo anaranjado

// Tonos oscuros para contraste:
val Charcoal = Color(0xFF333333)        // Fondo oscuro (gris oscuro con un toque más cálido)
val DarkSlate = Color(0xFF2F3B4C)       // Superficies elevadas (azul grisáceo oscuro)
val DarkRed = Color(0xFFB71C1C)         // Rojo oscuro (para resaltar elementos importantes)

// Neutros:
val OffWhite = Color(0xFFF5F5F5)        // Blanco suave para fondos más cómodos
val LightSlate = Color(0xFF788C99)      // Gris azulado suave, ideal para superficies elevadas
val Black = Color(0xFF000000)           // Texto negro para alto contraste


val Purple200 = Color(0xFFCE93D8)       // Morado suave y claro
val Purple700 = Color(0xFF6A1B9A)       // Morado oscuro vibrante
val Lavender500 = Color(0xFFE1BEE7)     // Acento lavanda suave
val Lavender600 = Color(0xFFD1C4E9)     // Lavanda ligeramente más saturada

val Magenta500 = Color(0xFFEC407A)      // Acento magenta cálido
val Magenta700 = Color(0xFFD81B60)      // Magenta más oscuro para contraste

// Tonos oscuros:
val DeepPurple = Color(0xFF311B92)      // Fondo oscuro profundo
val DarkPlum = Color(0xFF4A148C)        // Superficies elevadas (ciruela oscura)

// Neutros:
val Ivory = Color(0xFFFFF8E1)           // Blanco cálido para el fondo claro
val SoftGray = Color(0xFFE0E0E0)        // Gris claro para superficies
val Onyx = Color(0xFF212121)            // Negro profundo para alto contraste

val Yellow200 = Color(0xFFFFF59D)       // Amarillo pastel suave
val Yellow700 = Color(0xFFFBC02D)       // Amarillo oscuro dorado
val Gold500 = Color(0xFFFFD700)         // Acento en dorado brillante
val Gold600 = Color(0xFFFFC107)         // Dorado cálido más saturado

val Orange500 = Color(0xFFFF7043)       // Acento en naranja cálido
val Orange700 = Color(0xFFEF6C00)       // Naranja más oscuro para contraste

// Tonos oscuros:
val DeepBrown = Color(0xFF3E2723)       // Fondo oscuro marrón profundo
val RichAmber = Color(0xFF795548)       // Superficies elevadas (ámbar oscuro)

// Neutros:
val Vanilla = Color(0xFFFFF8E1)         // Blanco cálido para el fondo claro
val LightTan = Color(0xFFD7CCC8)        // Beige claro para superficies
val Midnight = Color(0xFF212121)        // Negro profundo para alto contraste

// Colores para el Tema Light de Equipo Rocket
val RocketPinkLight = Color(0xFFC2185B)         // Rosa vibrante (representando a Jessie)
val RocketPurpleLight = Color(0xFF6A1B9A)       // Morado oscuro (sombra de villanía)
val RocketGreyLight = Color(0xFF424242)         // Gris oscuro (aspecto frío y calculador)
val RocketWhiteLight = Color(0xFFFFFFFF)        // Fondo claro (claridad superficial)
val RocketLightGreyLight = Color(0xFFB0BEC5)    // Superficies suaves (aspecto limpio)
val RocketBlackLight = Color(0xFF000000)        // Texto negro sobre rosa (contrastante y audaz)
val RocketDarkPurpleLight = Color(0xFF512DA8)   // Texto en morado oscuro sobre fondo blanco

// Colores para el Tema Light de Equipo Magma
val MagmaRedLight = Color(0xFFD32F2F)            // Rojo oscuro (como el magma)
val MagmaOrangeLight = Color(0xFFFF9800)         // Naranja cálido (volcanes y fuego)
val MagmaBrownLight = Color(0xFF6D4C41)          // Marrón oscuro (tierra y magma)
val MagmaBeigeLight = Color(0xFFF5F5F5)          // Fondo cálido (color terroso, como la lava)
val MagmaLightBrownLight = Color(0xFF9E9D24)     // Superficies en tonos tierra
val MagmaWhiteLight = Color(0xFFFFFFFF)          // Texto blanco sobre rojo oscuro
val MagmaBlackLight = Color(0xFF000000)          // Texto negro sobre naranja cálido
val MagmaDarkBrownLight = Color(0xFF3E2723)      // Texto en marrón oscuro sobre fondo beige

// Colores para el Tema Light de Equipo Aqua
val AquaBlueLight = Color(0xFF1976D2)            // Azul profundo (como el mar)
val AquaTealLight = Color(0xFF00796B)            // Verde azulado (agua y océano)
val AquaCyanLight = Color(0xFF00BCD4)            // Cian fresco y claro (agua)
val AquaLightBlueLight = Color(0xFF81D4FA)       // Fondo claro y acuático
val AquaLightCyanLight = Color(0xFFB2EBF2)       // Superficies en tonos suaves de azul
val AquaWhiteLight = Color(0xFFFFFFFF)           // Texto blanco sobre azul
val AquaDarkBlueLight = Color(0xFF003366)        // Texto en azul oscuro sobre fondo claro

// Colores para el Tema Light de Equipo Galáctico
val GalacticDarkBlueLight = Color(0xFF0D47A1)    // Azul oscuro (espacio profundo)
val GalacticSilverLight = Color(0xFFB0BEC5)      // Plata metálica oscura
val GalacticPurpleLight = Color(0xFF9C27B0)      // Púrpura cósmico
val GalacticBlackLight = Color(0xFF000000)       // Fondo negro (como el espacio)
val GalacticDarkGreyLight = Color(0xFF616161)    // Superficies oscuras metálicas
val GalacticWhiteLight = Color(0xFFFFFFFF)       // Texto blanco sobre azul oscuro
val GalacticBlackTextLight = Color(0xFF000000)   // Texto negro sobre plata
val GalacticLightGreyLight = Color(0xFFB0BEC5)   // Texto gris claro sobre fondo negro

// Colores para el Tema Light de Equipo Plasma
val PlasmaGreenLight = Color(0xFF388E3C)         // Verde oscuro (naturaleza y poder)
val PlasmaBlackLight = Color(0xFF000000)         // Negro (rebeldía)
val PlasmaLimeLight = Color(0xFFCDDC39)          // Verde lima oscuro (energía)
val PlasmaWhiteLight = Color(0xFFFFFFFF)         // Fondo claro (balance de colores oscuros)
val PlasmaLightGreyLight = Color(0xFFB0BEC5)     // Superficies suaves en gris
val PlasmaDarkGreenLight = Color(0xFF388E3C)     // Texto verde oscuro sobre fondo blanco

// Colores para el Tema Light de Equipo Flare
val FlareRedLight = Color(0xFFF44336)            // Rojo fuego (intenso y dramático)
val FlareYellowLight = Color(0xFFFFEB3B)         // Amarillo cálido (fuego)
val FlareGoldLight = Color(0xFFFFC107)           // Dorado (lujo y ostentación)
val FlareDarkGreyLight = Color(0xFF212121)       // Fondo oscuro (serio y peligroso)
val FlareLightGreyLight = Color(0xFFB0BEC5)      // Superficies claras
val FlareWhiteLight = Color(0xFFFFFFFF)          // Texto blanco sobre rojo intenso
val FlareBlackLight = Color(0xFF000000)          // Texto negro sobre amarillo
val FlareDarkRedLight = Color(0xFFB71C1C)        // Texto en rojo oscuro sobre fondo oscuro

// Colores para el Tema Dark de Equipo Rocket
val RocketPinkDark = Color(0xFFC2185B)       // Rosa vibrante (para mantener la identidad)
val RocketPurpleDark = Color(0xFF6A1B9A)     // Morado oscuro
val RocketGreyDark = Color(0xFF212121)       // Gris oscuro
val RocketBackgroundDark = Color(0xFF4A148C) // Fondo morado oscuro
val RocketSurfaceDark = Color(0xFF424242)    // Superficies oscuras
val RocketOnPrimaryDark = Color(0xFF000000)  // Texto negro sobre rosa
val RocketOnSecondaryDark = Color(0xFFFFFFFF) // Texto blanco sobre morado oscuro
val RocketOnBackgroundDark = Color(0xFFFFFFFF) // Texto blanco sobre fondo morado

// Colores para el Tema Dark de Equipo Magma
val MagmaRedDark = Color(0xFFD32F2F)        // Rojo intenso (magma)
val MagmaOrangeDark = Color(0xFFFB8C00)     // Naranja cálido (fuego)
val MagmaBrownDark = Color(0xFF3E2723)      // Marrón oscuro
val MagmaBackgroundDark = Color(0xFFB71C1C) // Fondo rojo oscuro
val MagmaSurfaceDark = Color(0xFF3E1A2B)    // Superficies marrón oscuro
val MagmaOnPrimaryDark = Color(0xFFFFFFFF)  // Texto blanco sobre rojo
val MagmaOnSecondaryDark = Color(0xFF000000) // Texto negro sobre naranja
val MagmaOnBackgroundDark = Color(0xFF6D4C41) // Texto marrón claro sobre fondo oscuro

// Colores para el Tema Dark de Equipo Aqua
val AquaBlueDark = Color(0xFF1565C0)        // Azul oscuro (mar y océano profundo)
val AquaTealDark = Color(0xFF00796B)        // Verde azulado oscuro
val AquaCyanDark = Color(0xFF00BCD4)        // Cian profundo
val AquaBackgroundDark = Color(0xFF0D47A1)  // Fondo azul marino
val AquaSurfaceDark = Color(0xFF1565C0)     // Superficies azules oscuras
val AquaOnPrimaryDark = Color(0xFFFFFFFF)   // Texto blanco sobre azul oscuro
val AquaOnSecondaryDark = Color(0xFFFFFFFF) // Texto blanco sobre verde azulado oscuro
val AquaOnBackgroundDark = Color(0xFF81D4FA) // Texto azul claro sobre fondo oscuro

// Colores para el Tema Dark de Equipo Galáctico
val GalacticDarkBlueDark = Color(0xFF0D47A1) // Azul oscuro (el espacio profundo)
val GalacticSilverDark = Color(0xFF78909C)   // Plata metálica oscura
val GalacticPurpleDark = Color(0xFF4A148C)   // Púrpura muy oscuro (cósmico)
val GalacticBackgroundDark = Color(0xFF000000) // Fondo negro (como el espacio)
val GalacticSurfaceDark = Color(0xFF424242)   // Superficies oscuras metálicas
val GalacticOnPrimaryDark = Color(0xFFFFFFFF)  // Texto blanco sobre azul oscuro
val GalacticOnSecondaryDark = Color(0xFF000000) // Texto negro sobre plata
val GalacticOnBackgroundDark = Color(0xFFB0BEC5) // Texto gris claro sobre fondo negro

// Colores para el Tema Dark de Equipo Plasma
val PlasmaGreenDark = Color(0xFF1B5E20)      // Verde muy oscuro (naturaleza y poder)
val PlasmaBlackDark = Color(0xFF000000)      // Negro (oscuro y rebelde)
val PlasmaLimeDark = Color(0xFFCDDC39)       // Verde lima oscuro
val PlasmaBackgroundDark = Color(0xFF388E3C) // Fondo verde oscuro
val PlasmaSurfaceDark = Color(0xFF212121)    // Superficies grises oscuras
val PlasmaOnPrimaryDark = Color(0xFFFFFFFF)  // Texto blanco sobre verde muy oscuro
val PlasmaOnSecondaryDark = Color(0xFFFFFFFF) // Texto blanco sobre negro
val PlasmaOnBackgroundDark = Color(0xFF81C784) // Texto verde claro sobre fondo oscuro

// Colores para el Tema Dark de Equipo Flare
val FlareRedDark = Color(0xFFC62828)         // Rojo muy oscuro (fuego intenso)
val FlareYellowDark = Color(0xFFF57F17)      // Amarillo oscuro (fuego quemado)
val FlareGoldDark = Color(0xFFFBC02D)        // Dorado oscuro
val FlareBackgroundDark = Color(0xFF212121)  // Fondo gris oscuro (serio y peligroso)
val FlareSurfaceDark = Color(0xFF424242)     // Superficies grises
val FlareOnPrimaryDark = Color(0xFFFFFFFF)   // Texto blanco sobre rojo oscuro
val FlareOnSecondaryDark = Color(0xFF000000) // Texto negro sobre amarillo oscuro
val FlareOnBackgroundDark = Color(0xFFD32F2F) // Texto rojo sobre fondo gris oscuro
